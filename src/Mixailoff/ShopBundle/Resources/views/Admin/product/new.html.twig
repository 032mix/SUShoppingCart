{% extends 'base.html.twig' %}

{% block body %}
    <div align="center">
        <div style="width: 300px;">
            <h1>Product creation</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create"/>
            {{ form_end(form) }}

            <a href="{{ path('edit_product_index') }}">Back to the list</a><br>
        </div>
    </div>
{% endblock %}
