{% extends 'base.html.twig' %}

{% block body %}
    <div align="center">
        <div style="width: 300px;">
            <h1>Productcategory creation</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create"/>
            {{ form_end(form) }}

            <a href="{{ path('edit_productcategory_index') }}">Back to the list</a><br>
        </div>
    </div>
{% endblock %}
