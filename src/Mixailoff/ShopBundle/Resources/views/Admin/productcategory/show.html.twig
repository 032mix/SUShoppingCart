{% extends 'base.html.twig' %}

{% block body %}
    <div align="center">
        <h1>Productcategory</h1>

        <table>
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ productCategory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ productCategory.name }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('edit_productcategory_index') }}">Back to the list</a><br>

        <a href="{{ path('edit_productcategory_edit', { 'id': productCategory.id }) }}">Edit</a><br>

        {{ form_start(delete_form) }}
        <input type="submit" value="Delete">
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
